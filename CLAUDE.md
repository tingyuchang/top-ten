# Top Ten 出題網站

仿桌遊《腦洞量表 Top Ten》的出題網頁。評分由玩家用實體撲克牌（1-10）完成，
網站只負責出題，不處理計分。部署在 GitHub Pages，開網址就能用。

## 結構

- `index.html` — 整個網站。HTML / CSS / JS 全部內嵌在這一個檔案裡。
- `questions.json` — 題庫，直接編輯即可增刪題目。
- `background.jpeg` — 背景圖，1600px 寬。新圖請先 `sips -Z 1600` 壓過再放進來。

純靜態，沒有建置流程、沒有相依套件、沒有後端。改完直接 push 就上線。

## 題庫

每題的欄位：`id`、`text`、`category`、`lowLabel`（1 代表的意思）、
`highLabel`（10 代表的意思）。

現有分類與 id 前綴：

| category | 前綴 | 說明 |
|---|---|---|
| `mild` | `m` | 溫和向 |
| `adult` | `a` | 成人向 |
| `family` | `f` | 親子向，適合大人跟小孩一起玩 |
| `313` | `s` | 313 班專屬，同學過去相處經驗 |

新增分類要做兩件事：題庫加題目、`index.html` 的 `#filters` 加一個
`<label><input type="checkbox" value="<category>" checked> 顯示名稱</label>`。
抽題邏輯讀勾選框的 `value`，不需要改 JS。

## 行為

- 抽題從勾選分類的題目池隨機取，排除本局已抽過的（記憶體 `Set`，重整 = 新的一局）。
- 池子空了顯示「題目已抽完，可重置後再抽」，「重置本局」清空記錄。
- 卡片顯示題目 + 「1 是 X，10 是 Y」。

## 風格

Party game 氛圍：鮮明色彩、卡片式、抽題有動效。抽題按鈕是視覺焦點，
要大、好點。手機瀏覽器優先，卡片要在小螢幕上清楚易讀。

## 不做

計分/排序、跨裝置同步、帳號系統、後端 API、資料庫、多人連線。

## 慣例

- 介面文字與題目一律用繁體中文。
- 保持單檔：不要為了加功能就拆出 `.css` / `.js`，也不要引入框架或套件。
